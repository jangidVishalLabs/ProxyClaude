# Installs run inside WSL (native ext4). pnpm defaults work there.
# Do NOT run pnpm/npm from the Windows side over \\wsl.localhost — its
# symlink/hardlink/CoW semantics break workspace linking.
engine-strict=true
